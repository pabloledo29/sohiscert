{% extends 'base_full.html.twig' %}

{% block title %}Editar mapeo del número de expediente{% endblock %}

{% block content %}
<div class="box box-info">
                    <div class="box-header">
                        <i class="fa fa-user"></i>

                        <h3 class="box-title">Mapa de número de expediente</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
    <h1>Editar mapeo del número de expediente</h1>

    {{ include('nop_transform/_form.html.twig', {'button_label': 'Actualizar'}) }}
    {{ include('nop_transform/_delete_form.html.twig') }}

    <div  style="margin:1em 0;">
        <a class="btn btn-success" href="{{ path('nop_transform_index') }}">Volver al listado</a>
    </div>
    
    </div>
    </div>
{% endblock %}
